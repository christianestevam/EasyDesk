version: '3.7'

services:
  postgres:
    image: postgres:latest
    container_name: postgres_database2
    environment:
      POSTGRES_DB: easydesk_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"